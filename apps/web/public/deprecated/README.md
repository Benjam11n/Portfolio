Deprecated public media.

- `models/` is no longer used. The site no longer serves the old 3D model assets.
- `original-media/` is no longer used. The site now uses more efficient compressed replacements.

Keep these files only for archival/reference purposes unless they are explicitly restored.
