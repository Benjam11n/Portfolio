commit-msg:
  jobs:
    - name: commitlint
      run: pnpm commitlint --edit {1}

pre-commit:
  parallel: true
  jobs:
    - name: lint
      run: pnpm lint

    - name: typecheck
      run: pnpm typecheck
