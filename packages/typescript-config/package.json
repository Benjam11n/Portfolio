{
  "name": "@repo/typescript-config",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "clean": "rm -rf .cache .turbo dist node_modules",
    "type-check": "tsgo --project ./tsconfig.base-check.json --noEmit --emitDeclarationOnly false && tsgo --project ./tsconfig.nextjs-check.json --noEmit --emitDeclarationOnly false && tsgo --project ./tsconfig.react-library-check.json --noEmit --emitDeclarationOnly false"
  }
}
